{
  "fragmentId": "dev-doc-f4",
  "docId": "dev-doc",
  "order": 4,
  "title": "四阶段MCP项目管理工具 - 开发文档(2/4)",
  "content": "interface Edge {\n  edgeId: string;\n  from: string;\n  to: string;\n  label?: string;\n  payload?: Record<string, any>;\n}\n\ninterface Group {\n  groupId: string;\n  title: string;\n  axis?: \"vertical\" | \"horizontal\"; // 纵向=模块/层级；横向=阶段/泳道；默认 vertical\n  nodeIds: string[];\n  parentGroupId?: string;  // 纵向分区支持嵌套\n  collapsible?: boolean;   // 是否可折叠，仅纵向分区（模块）有效\n}\n```\n\n#### 6.3.5 双轴分区与模块折叠设计\n\n**双轴分区：一个节点可从两个维度获得上下文**\n\n- **纵向分区**（`axis: vertical`）＝模块/层级，表达节点\"属于哪个模块\"，支持 `parentGroupId` 嵌套与 `collapsible` 折叠\n- **横向分区**（`axis: horizontal`）＝阶段/泳道，表达节点\"处于哪部分\"；一个节点可同时归属一个纵向模块与一个横向分区\n- 节点**前后顺序**不新增字段，复用 `edges` 的 from→to 拓扑序，由布局引擎在横向分区内按拓扑序排布\n\n**模块折叠（聚焦渲染）**\n\n- 大图按模块分区后，聚焦查看某模块内部时，其余模块折叠为**聚合节点**\n- 聚合节点摘要仅包含：**模块名 + 子节点数**\n- **跨模块连线**折叠后在聚合节点上**收敛连线**，不展示内部细节\n- 折叠/展开状态为纯视图状态，**仅前端会话内生效**，不持久化、不进入 MCP 通道\n- 布局引擎输入增加聚焦模块参数，仅对聚焦模块内部做局部布局，其余模块输出为聚合占位节点\n\n**节点模型设计约定**\n\n1. 三种图节点为独立强类型，后端用 Zod discriminated union 按 `type` 区分校验，防止跨类型字段混用\n2. 类图属性/方法在业务层结构化展开，AI 可按字段精确读写，不做 payload 兜底\n3. 业务层禁止出现坐标、尺寸、形状、颜色等视觉字段；视觉呈现完全由布局引擎与前端决定\n4. 分区/折叠均为业务语义或视图状态，折叠状态禁止持久化\n\n### 6.4 LayoutDiagram 布局结果模型（内部渲染用，不持久化、不走MCP）\n\n```\ninterface LayoutDiagram {\n  diagramId: string;\n  width: number;\n  height: number;\n  nodes: Array<{\n    nodeId: string;\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n  }>;\n  edges: Array<{\n    edgeId: string;\n    points: Array<{ x: number; y: number }>;\n  }>;\n  groups: Array<{\n    groupId: string;\n    x: number;\n    y: number;\n    width: number;\n    height: number;\n  }>;\n}\n```\n\n### 6.5 ProjectIndex 项目索引（降Token核心）\n\n```\ninterface ProjectIndex {\n  projectId: string;\n  version: number;\n  documentIndex: Array<{\n    docId: string;\n    title: string;\n    summary: string;\n    fragmentIds: string[];\n  }>;\n  diagramIndex: Array<{\n    diagramId: string;\n    title: string;\n    type: string;\n    nodeCount: number;\n    edgeCount: number;\n  }>;\n  requirementIndex: Array<{       // 需求级索引（第一级）\n    requirementId: string;\n    title: string;\n    status: string;\n    branchName?: string;\n    tas"
}