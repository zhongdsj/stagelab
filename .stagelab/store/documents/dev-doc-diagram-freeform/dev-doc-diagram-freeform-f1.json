{
  "fragmentId": "dev-doc-diagram-freeform-f1",
  "docId": "dev-doc-diagram-freeform",
  "order": 1,
  "title": "五、技术选型",
  "content": "## 五、技术选型\n\n1. **渲染方案**：维持 Vue3 + SVG（DiagramSvg.vue），不引入第三方画布库（如 drawio/antv X6），减少依赖与迁移成本；\n2. **坐标数据形态**：在 Diagram 语义模型上扩展 geometry 字段，节点持有 x/y/width/height，连线持有 points；渲染层**统一以 geometry/points 为唯一取数来源**；\n3. **缺省默认值**：图数据缺 geometry/points 时，用布局引擎计算结果作为默认值补齐（节点默认尺寸/位置、连线默认正交折线），并固化为图数据；\n4. **布局引擎**：保留 ELK（layout/engine.ts）仅作「首次生成默认值」与「重新自动布局」入口；\n5. **持久化**：节点/连线坐标与折点作为图数据一部分，通过服务端 updateDiagramElements / 批量 geometry 接口落库，替代现有 localStorage 覆盖（useManualEdit）；\n6. **交互实现**：复用现有 PointerEvent 拖拽框架（clientToWorld/viewBox 换算），扩展为支持节点整体拖拽、连线线段平移、折点增删。\n\n## 六、数据模型设计\n\n### 6.1 节点坐标（Node geometry）\n\n在现有节点类型（ArchitectureNode / ClassNode / FlowNode）上扩展可选字段：\n\n```ts\ninterface NodeGeometry {\n  x: number;\n  y: number;\n  width: number;\n  height: number;\n}\n```\n\n各节点接口增加 `geometry?: NodeGeometry`。\n\n### 6.2 连线折点（Edge points）\n\n```ts\ninterface Edge {\n  edgeId: string;\n  from: string;\n  to: string;\n  label?: string;\n  payload?: Record<string, unknown>;\n  points?: Array<{ x: number; y: number }>;\n}\n```\n\n- points[0] 为 from 端锚点、points[last] 为 to 端锚点（绝对世界坐标，随节点移动联动重算）；\n- 无 points 时由引擎生成默认正交折线补齐。\n\n### 6.3 统一渲染 + 缺省默认值补齐（核心，替代原双态设计）\n\n- 渲染层**不做新旧双态**：所有图统一按 geometry/points 渲染；\n- 缺省默认值规则（幂等补齐，逐元素判断）：\n  - 节点无 geometry → 用引擎计算的默认尺寸与位置补齐（类图/流程图按内容计算宽高）；\n  - 连线无 points → 用引擎生成的正交折线补齐；\n- 首次打开检测到任一缺省 → 补齐全部默认值并一次性固化到图数据，此后与新图行为一致；\n- 补齐动作幂等：重复打开不重复产生脏数据，后续拖拽以固化数据为准。\n\n### 6.4 分组与折叠\n\n分组（Group）仅保留语义成员关系，不固化坐标；折叠/泳道等结构仍由渲染层基于节点 geometry 推导。模块框包围盒改为由成员节点几何动态计算。"
}