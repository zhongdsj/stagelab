# 四阶段MCP项目管理工具 - 开发文档

**文档版本**：v1.1
**阶段**：阶段1 - 需求定义与方案设计
**技术栈**：Node.js + TypeScript 后端 + Vue3 SPA 前端
**核心特性**：纯结构化JSON图元、原生布局引擎、零Mermaid依赖、MCP标准协议、索引分片降Token、Git仓库嵌入式存储

------

## 一、业务背景

现有一套四阶段可回溯开发工作流，覆盖需求讨论→任务拆解→编码实现→Bug修复全链路。当前缺少统一的本地载体工具来管理全流程产物，且AI与工具的交互依赖大段文本传输，Token开销高、结构化能力弱。

本项目目标是构建一款本地运行的项目管理工具：

1. 以「项目」为核心实体，统一承载四阶段全部产物与数据
2. 对外暴露标准MCP服务，供AI客户端调用执行全流程操作
3. 采用纯结构化JSON描述架构图、类图、流程图，后端完成布局计算，前端直接渲染可视化
4. 内置索引与分片机制，从底层设计降低MCP交互的Token消耗
5. 同时提供Web可视化界面，供人工查看、编辑与流程管控

------

## 二、功能目标

### 2.1 项目实体管理

- 项目创建、打开、列表查询、阶段切换
- 一个项目绑定一套完整的四阶段工作流，所有数据归属对应项目
- 项目元数据包含：项目ID、名称、当前阶段、创建时间、各阶段资源索引

### 2.2 四阶段产物管理

1. **阶段1**：开发文档持久化、架构图结构化存储与渲染、开发约束持久化
2. **阶段2**：任务文档拆解、任务清单管理、类图/流程图结构化存储与渲染
3. **阶段3**：任务完成状态标记
4. **阶段4(暂定)**：Bug记录、修复方案、回归校验记录

### 2.3 结构化图与可视化

- 支持三种图类型：架构图、类图、流程图
- AI与MCP仅读写**业务语义JSON结构**（节点、连线、分组，无坐标）
- 后端内置布局引擎，自动计算节点坐标与连线路径
- 前端基于布局结果直接渲染原生SVG，不依赖Mermaid
- 支持图元局部增删改，无需全量传输整张图

### 2.4 MCP服务

- 实现标准MCP协议，支持stdio接入模式
- 提供项目管理、文档读写、图元操作、索引查询四大类能力
- 所有接口支持细粒度操作，禁止全量拉取大对象

### 2.5 Web前端界面

- 项目总览面板、四阶段流程导航
- 文档查看与编辑、图表可视化渲染
- 任务清单管理、状态追踪
- 纯浏览器访问，无需安装桌面客户端

### 2.6 Token消耗优化

- 项目级索引层，存储摘要与ID映射
- 文档分片存储，支持按片段读取
- 图元支持按ID局部读取与局部更新
- 布局坐标数据仅内部流转，不进入MCP通道

------

## 三、约束条件

1. **本地运行**：所有数据本地文件持久化，不依赖云端服务
2. **协议标准**：严格遵循MCP协议规范，兼容标准MCP客户端
3. **零Mermaid原则**：系统全链路不出现、不解析、不存储Mermaid文本
4. **数据分层**：业务语义数据与渲染布局数据严格分离，MCP仅传输业务层
5. **索引优先**：任何批量查询必须先经过索引，禁止直接全量加载项目数据
6. **技术统一**：前后端全TypeScript实现，共享类型定义与Schema校验

------

## 四、技术选型

| 层级       | 技术选型                  | 说明                                     |
| ---------- | ------------------------- | ---------------------------------------- |
| 后端运行时 | Node.js 18+               | 全量业务逻辑承载                         |
| 后端语言   | TypeScript 5.x            | 类型安全，前后端共享类型                 |
| MCP服务    | @modelcontextprotocol/sdk | 标准MCP协议实现                          |
| HTTP服务   | Fastify                   | 轻量高性能，提供前端API                  |
| Schema校验 | Zod                       | 统一数据模型校验                         |
| 图布局引擎 | @dagrejs/elk              | 输入结构化JSON，输出带坐标布局结果       |
| 前端框架   | Vue 3 + Vite              | SPA单页应用                              |
| 图表渲染   | 原生SVG                   | 基于布局坐标直接绘制，无第三方图表库依赖 |
| 本地存储   | 文件系统(JSON)            | 每个项目独立目录，分文件存储             |
| 异步计算   | Node Worker Threads       | 隔离大图布局计算，避免阻塞主线程         |

------

## 五、系统架构

### 5.1 整体架构

```
┌─────────────────┐          stdio          ┌───────────────────────────────┐
│  MCP Client(AI) │◄───────────────────────►│                               │
└─────────────────┘                           │       Node.js 后端           │
                                              │  ┌─────────────────────────┐  │
                                              │  │      MCP Server         │  │
                                              │  └───────────┬─────────────┘  │
                                              │              │                │
┌─────────────────┐          HTTP            │  ┌───────────▼─────────────┐  │
│  Web 前端(浏览器)│◄───────────────────────►│  │      HTTP API层          │  │
└─────────────────┘                           │  └───────────┬─────────────┘  │
                                              │              │                │
                                              │  ┌───────────▼─────────────┐  │
                                              │  │   业务服务层             │  │
                                              │  │  - 项目管理服务          │  │
                                              │  │  - 文档分片服务          │  │
                                              │  │  - 图元CRUD服务          │  │
                                              │  │  - 索引服务              │  │
                                              │  └───────────┬─────────────┘  │
                                              │              │                │
                                              │  ┌───────────▼─────────────┐  │
                                              │  │   布局引擎(Worker)       │  │
                                              │  │   elk计算节点坐标        │  │
                                              │  └───────────┬─────────────┘  │
                                              │              │                │
                                              │  ┌───────────▼─────────────┐  │
                                              │  │   本地文件存储层         │  │
                                              │  │   项目目录/JSON文件      │  │
                                              │  └─────────────────────────┘  │
                                              └───────────────────────────────┘
```

### 5.2 核心数据流

1. AI通过MCP发送操作请求 → MCP Server解析 → 调用对应业务服务
2. 业务服务操作本地JSON文件 → 返回业务语义数据（无坐标）
3. 前端请求图表渲染 → HTTP API调用布局引擎 → 输出带坐标的布局结果 → 前端SVG渲染
4. 索引查询优先返回轻量摘要 → 按需再拉取完整片段

------

## 六、核心数据模型

### 6.1 Project 项目实体

```
interface Project {
  projectId: string;
  projectName: string;
  currentStage: "s1" | "s2" | "s3" | "s4";
  createdAt: number;
  updatedAt: number;
  stage1: {
    docId: string;
    diagramIds: string[];
  };
  stage2: {
    taskDocId: string;
    requirementIds: string[];   // 需求列表（需求下挂任务）
    diagramIds: string[];
  };
  stage3: {
    changeRecordIds: string[];
  };
  stage4: {
    bugRecordIds: string[];
  };
  indexId: string;
}
```

### 6.2 Requirement 需求实体（阶段2任务分组粒度）

任务按「需求」这一更粗粒度分组管理，一个需求可关联一个 Git 分支，task 跟随分支切换。

```
interface Requirement {
  requirementId: string;
  title: string;                       // 需求标题
  description?: string;                // 需求描述
  branchName?: string;                 // 关联 Git 分支名（可选）
  status: "active" | "done" | "archived"; // 需求生命周期状态
  taskIds: string[];                   // 该需求下的任务ID列表
  createdAt: number;
  updatedAt: number;
}
```

- 层级：`Project → Requirement → Task`，需求为独立结构化实体
- 需求关联 Git 分支：`branchName` 与仓库分支对齐，切分支时该需求及其 task 跟随切换
- 需求状态独立：需求完成可 `done`/`archived` 归档，不影响其他需求
- AI 读取路径：需求索引 → 需求内 task 摘要 → 单 task，Token 分层消耗

### 6.3 Diagram 业务图模型（MCP读写、持久化存储）

三种图共用同一图容器结构，`nodes` 类型随 `type` 而定，采用**独立强类型节点**定义，业务层**纯语义不含视觉信息**（坐标、形状、颜色一律不进入业务模型）。

```
// 图容器（三种图通用）
interface Diagram {
  diagramId: string;
  type: "architecture" | "class" | "flow";
  metadata: {
    title: string;
    version: number;
    description?: string;
  };
  nodes: ArchitectureNode[] | ClassNode[] | FlowNode[]; // 按 type 对应
  edges: Edge[];
  groups: Group[];
}
```

#### 6.3.1 架构图节点 ArchitectureNode

```
interface ArchitectureNode {
  nodeId: string;
  label: string;                              // 组件显示名称
  layer?: string;                             // 所属层级：接入层/服务层/数据层…
  nodeKind?: "service" | "database" | "mq"
            | "cache" | "external" | "gateway"; // 组件类型
  description?: string;                       // 组件职责说明
  payload?: Record<string, any>;              // 扩展信息：端口、协议、依赖版本等
}
```

#### 6.3.2 类图节点 ClassNode（属性/方法结构化展开）

```
interface ClassNode {
  nodeId: string;
  label: string;                              // 类名
  kind?: "class" | "interface" | "abstract" | "enum";
  attributes?: ClassAttribute[];              // 属性结构化列表
  methods?: ClassMethod[];                    // 方法结构化列表
  description?: string;                       // 类职责说明
}

interface ClassAttribute {
  name: string;
  type: string;
  visibility?: "public" | "private" | "protected";
  static?: boolean;
  description?: string;
}

interface ClassMethod {
  name: string;
  params?: Array<{ name: string; type: string }>;
  returnType?: string;
  visibility?: "public" | "private" | "protected";
  static?: boolean;
  abstract?: boolean;
  description?: string;
}
```

#### 6.3.3 流程图节点 FlowNode

```
interface FlowNode {
  nodeId: string;
  label: string;                              // 步骤名称
  nodeKind?: "start" | "end" | "process"
            | "decision" | "inputOutput" | "subprocess"; // 节点类型
  description?: string;                       // 步骤说明
  payload?: Record<string, any>;              // 扩展信息：判断条件、超时等
}
```

#### 6.3.4 连线与分组（三种图通用）

```
interface Edge {
  edgeId: string;
  from: string;
  to: string;
  label?: string;
  payload?: Record<string, any>;
}

interface Group {
  groupId: string;
  title: string;
  axis?: "vertical" | "horizontal"; // 纵向=模块/层级；横向=阶段/泳道；默认 vertical
  nodeIds: string[];
  parentGroupId?: string;  // 纵向分区支持嵌套
  collapsible?: boolean;   // 是否可折叠，仅纵向分区（模块）有效
}
```

#### 6.3.5 双轴分区与模块折叠设计

**双轴分区：一个节点可从两个维度获得上下文**

- **纵向分区**（`axis: vertical`）＝模块/层级，表达节点"属于哪个模块"，支持 `parentGroupId` 嵌套与 `collapsible` 折叠
- **横向分区**（`axis: horizontal`）＝阶段/泳道，表达节点"处于哪部分"；一个节点可同时归属一个纵向模块与一个横向分区
- 节点**前后顺序**不新增字段，复用 `edges` 的 from→to 拓扑序，由布局引擎在横向分区内按拓扑序排布

**模块折叠（聚焦渲染）**

- 大图按模块分区后，聚焦查看某模块内部时，其余模块折叠为**聚合节点**
- 聚合节点摘要仅包含：**模块名 + 子节点数**
- **跨模块连线**折叠后在聚合节点上**收敛连线**，不展示内部细节
- 折叠/展开状态为纯视图状态，**仅前端会话内生效**，不持久化、不进入 MCP 通道
- 布局引擎输入增加聚焦模块参数，仅对聚焦模块内部做局部布局，其余模块输出为聚合占位节点

**节点模型设计约定**

1. 三种图节点为独立强类型，后端用 Zod discriminated union 按 `type` 区分校验，防止跨类型字段混用
2. 类图属性/方法在业务层结构化展开，AI 可按字段精确读写，不做 payload 兜底
3. 业务层禁止出现坐标、尺寸、形状、颜色等视觉字段；视觉呈现完全由布局引擎与前端决定
4. 分区/折叠均为业务语义或视图状态，折叠状态禁止持久化

### 6.4 LayoutDiagram 布局结果模型（内部渲染用，不持久化、不走MCP）

```
interface LayoutDiagram {
  diagramId: string;
  width: number;
  height: number;
  nodes: Array<{
    nodeId: string;
    x: number;
    y: number;
    width: number;
    height: number;
  }>;
  edges: Array<{
    edgeId: string;
    points: Array<{ x: number; y: number }>;
  }>;
  groups: Array<{
    groupId: string;
    x: number;
    y: number;
    width: number;
    height: number;
  }>;
}
```

### 6.5 ProjectIndex 项目索引（降Token核心）

```
interface ProjectIndex {
  projectId: string;
  version: number;
  documentIndex: Array<{
    docId: string;
    title: string;
    summary: string;
    fragmentIds: string[];
  }>;
  diagramIndex: Array<{
    diagramId: string;
    title: string;
    type: string;
    nodeCount: number;
    edgeCount: number;
  }>;
  requirementIndex: Array<{       // 需求级索引（第一级）
    requirementId: string;
    title: string;
    status: string;
    branchName?: string;
    taskCount: number;
  }>;
  taskIndex: Array<{             // 任务级索引（第二级，关联需求）
    taskId: string;
    title: string;
    status: string;
    requirementId: string;       // 归属需求
  }>;
}
```

> 两级索引：AI 先查 requirementIndex（轻量，需求+任务数）→ 按需查 taskIndex → 再拉单 task。符合分层访问机制。

### 6.6 DocumentFragment 文档分片

```
interface DocumentFragment {
  fragmentId: string;
  docId: string;
  order: number;
  title: string;
  content: string;
}
```

------

## 七、存储设计

### 7.1 Git仓库嵌入式存储（核心定位）

工具生成的四阶段产物（项目meta、索引、diagram图、文档分片、任务记录）**不与业务源码混在一起**，存放在**业务仓库根目录下的独立隐藏目录 `.stagelab/`**。

> 关键定位：工具数据**跟随业务 Git 仓库走**，源码、设计文档、结构图、任务记录一起分支切换、一起 commit。不使用全局统一 projects 文件夹。

**每个 Git 仓库最多对应一个工具 Project 项目；一个仓库 = 一套四阶段工程。**

### 7.2 目录结构（嵌入真实Git仓库）

```
your-git-repo/               # 真实业务git仓库根
├── src/                     # 业务源码（git正常管理）
├── .stagelab/              # 工具专属目录，git仓库内
│   ├── project.meta.json    # 【仓库级入口文件】本仓库对应的工具项目元入口
│   ├── store/               # 本仓库全部四阶段产物存储目录
│   │   ├── meta.json        # Project元数据
│   │   ├── index.json       # ProjectIndex索引
│   │   ├── documents/
│   │   │   └── {docId}.json
│   │   ├── diagrams/
│   │   │   └── {diagramId}.json
│   │   ├── requirements/
│   │   │   └── {requirementId}.json  # 需求实体
│   │   ├── tasks/
│   │   │   └── {taskId}.json
│   │   └── records/
│   │       ├── {changeId}.json
│   │       └── {bugId}.json
│   └── .write.lock          # 写入互斥锁，短期持有
├── .gitignore
└── ...业务代码
```

### 7.3 仓库入口文件 project.meta.json

路径：`{仓库根}/.stagelab/project.meta.json`

```
interface RepoEntryMeta {
  schemaVersion: string;   // 工具schema版本，用于后续迁移升级
  projectId: string;       // 本仓库对应的工具projectId
  storeDir: string;        // 相对路径，指向store目录: "./store"
  createdAt: number;
}
```

工作方式：

1. 用户打开某业务仓库，执行工具初始化：在仓库根生成 `.stagelab/project.meta.json`，初始化 store 下整套目录
2. Node 服务接收参数 `--repo /path/to/your-git-repo`，拼接得到 `.stagelab/project.meta.json`，加载对应项目
3. 支持同时打开多个仓库实例，每个仓库一套独立项目

### 7.4 文件IO强制规范（解决Git checkout冲突）

> 风险：Node fs.open 之后不 close 持续持有 fd，当执行 git checkout / git reset，Git 尝试替换文件会报资源占用，Windows 尤其严重。

1. **禁止长期持有文件句柄**：所有文件 读→open→read→立即close；写→open→write→立即close。全程不常驻 fd。
2. **内存不做永久缓存**：可做短时内存缓存（2-5s TTL）降低重复 IO，TTL 到期强制重读磁盘。
3. **外部变更检测（mtime）**：Git 切换分支会修改磁盘 JSON。每次读取前比较磁盘 mtime 与缓存 mtime，磁盘较新则丢弃缓存重新加载。
4. **安全写入（防 JSON 损坏）**：禁止直接覆盖原文件，采用 ①写临时文件 `xxx.tmp-{随机串}.json` ②完整序列化+fsync 落盘 ③原子 rename 覆盖正式文件 ④删除临时文件。
5. **文件锁（短持有）**：`.write.lock` 仅写入期间短暂持有，写完立刻释放；锁只做互斥不常驻。
6. **冲突拒绝**：拿到锁后发现磁盘 mtime 大于内存版本（外部 Git 已改），本次写入直接拒绝返回冲突，提示刷新重试，避免旧内存数据覆盖磁盘新数据。

### 7.5 存储规则

1. 所有数据采用JSON格式明文存储，便于人工查看与版本管理
2. 大文档强制分片存储，单分片内容不超过2000字
3. 布局结果不持久化，每次渲染实时计算
4. 索引文件随对应数据变更自动同步更新
5. 设计资产（文档、图、任务）推荐纳入 Git 版本管理，跟随分支切换

### 7.6 Git集成建议与.gitignore

**建议**：`.stagelab/store/**` 纳入 Git，设计文档、结构图、任务记录跟随业务代码一起版本管理、跟着分支走。

```
# four-stage 工具：可选项——若不想提交设计资产，放开下面两行
# .stagelab/store/**
# !.stagelab/project.meta.json

# 临时写入文件、缓存
.stagelab/*.tmp
.stagelab/**/*.tmp
```

### 7.7 Git切换分支与冲突处理

1. Git 切换分支时磁盘 `.stagelab/store/` 被替换，Node 服务不接收 Git 事件，靠 mtime 机制检测磁盘变动
2. 业务层现象：MCP 读取自动加载分支对应文档、图、任务；项目的 currentStage 也跟随分支切换
3. **工具不封装、不调用 Git 命令**：不做 git add/commit；提交、合并、冲突解决交给用户
4. **Git merge 冲突**：工具不做 JSON 冲突合并；冲突标记文件 Zod 校验失败，MCP/HTTP 返回「数据文件格式损坏，请手动解决 git 冲突后继续」

### 7.8 对原有模型与MCP接口的影响

1. Project 不再全局独立，**绑定到一个 Git 仓库**
2. MCP 工具增加切换工作仓库的参数，所有操作基于当前选中仓库
3. 读取服务层增加 mtime 判断；写入层实现 临时文件+原子rename+短时文件锁
4. Zod 校验失败区分两类错误：文件不存在 / JSON 格式损坏（大概率 Git 冲突）

------

## 八、MCP接口设计

### 8.1 项目管理类

| 工具名                 | 功能说明                                        |
| ---------------------- | ----------------------------------------------- |
| init_repo_project      | 初始化仓库：在仓库根生成 project.meta.json 与 store 目录 |
| set_working_repo       | 切换当前工作仓库（多仓库实例间切换）           |
| get_working_repo       | 获取当前工作仓库信息                           |
| create_project         | 创建新项目，初始化目录结构与索引，默认进入阶段1 |
| list_projects          | 获取项目列表（仅返回元信息，不包含详情）       |
| get_project_meta       | 获取单个项目元数据                              |
| switch_project_stage   | 切换项目当前阶段                                |

### 8.2 文档操作类

| 工具名                  | 功能说明                             |
| ----------------------- | ------------------------------------ |
| get_document_index      | 获取文档索引列表（轻量，仅标题摘要） |
| read_document_fragment  | 读取指定文档的指定分片内容           |
| write_document_fragment | 写入/更新文档分片                    |
| create_document         | 创建新文档                           |

### 8.3 需求与任务管理类

| 工具名                  | 功能说明                                         |
| ----------------------- | ------------------------------------------------ |
| create_requirement      | 创建需求（可关联分支名），进入阶段2任务分组      |
| list_requirements       | 获取需求列表（轻量：标题/状态/分支/任务数）      |
| get_requirement         | 获取需求详情（含任务摘要列表）                   |
| update_requirement      | 更新需求（状态切换 done/archived、分支名等）     |
| create_task             | 在指定需求下创建任务                             |
| update_task_status      | 更新任务完成状态                                 |
| list_tasks_by_requirement | 获取指定需求下的任务列表（轻量摘要）           |

### 8.4 图元操作类

| 工具名                  | 功能说明                                     |
| ----------------------- | -------------------------------------------- |
| create_diagram          | 创建新的结构化图                             |
| get_diagram_meta        | 获取图元数据（标题、类型、节点数，不含详情） |
| get_diagram_partial     | 按节点ID/连线ID读取部分图元                  |
| update_diagram_elements | 局部新增/修改/删除图元节点、连线、分组       |
| delete_diagram          | 删除整张图                                   |

### 8.5 索引查询类

| 工具名                 | 功能说明                                           |
| ---------------------- | -------------------------------------------------- |
| get_project_index      | 获取项目完整索引（轻量摘要，含需求/任务两级）      |
| search_project_content | 按关键词搜索项目内文档、图元、需求、任务，返回匹配ID列表 |

------

## 九、HTTP接口设计（前端专用）

| 方法 | 路径                                              | 功能               |
| ---- | ------------------------------------------------- | ------------------ |
| GET  | /api/projects                                     | 项目列表           |
| POST | /api/projects                                     | 创建项目           |
| GET  | /api/projects/:id                                 | 项目详情           |
| PUT  | /api/projects/:id/stage                           | 切换阶段           |
| GET  | /api/projects/:id/index                           | 获取项目索引       |
| GET  | /api/projects/:id/diagrams/:did/layout            | 计算并返回布局结果 |
| GET  | /api/projects/:id/documents/:docId/fragments      | 文档分片列表       |
| GET  | /api/projects/:id/documents/:docId/fragments/:fid | 读取分片           |

------

## 十、Token消耗优化方案

### 10.1 分层访问机制

- **第一层：索引层**：所有查询先访问索引，仅返回ID与摘要，Token开销降低80%以上
- **第二层：分片层**：按需读取文档分片、图元局部，避免全量加载
- **第三层：完整层**：仅在明确需要时才返回完整对象

### 10.2 数据裁剪

- MCP接口永远不返回布局坐标数据
- 列表接口只返回必要字段，禁止返回大文本内容
- 图元查询支持字段过滤，可指定只返回nodeId与label

### 10.3 增量更新

- 所有写操作支持局部patch，只传输变更部分
- 图元更新支持单节点、单连线修改，无需重传整张图
- 文档按分片更新，不重写全文

------

## 十一、风险点与应对

| 风险点                 | 影响                  | 应对方案                                       |
| ---------------------- | --------------------- | ---------------------------------------------- |
| 大图布局计算阻塞主线程 | MCP请求卡顿、接口超时 | 布局计算移入Worker Thread，异步执行，支持取消  |
| Node持有文件句柄，Git checkout报错 | Git切换分支/重置报资源占用 | 读写立刻close fd，绝不常驻打开文件 |
| Git切换分支读到旧脏数据 | 内存缓存过期失效 | 文件记录mtime，读取对比磁盘mtime过期重载；内存短TTL缓存 |
| 并发写入/Git打断导致JSON损坏 | 数据丢失 | 临时文件+fsync+原子rename替换，禁止原地覆写 |
| 多进程同时修改同一仓库数据 | 写入冲突 | 短期文件锁写完即释放；检测到磁盘已变更拒绝覆盖 |
| Git合并产生JSON冲突 | 文件带冲突标记无法解析 | 工具不自动合并；Zod校验报错，提示人工解决冲突 |
| 图元ID冲突             | 局部更新失败          | 全局ID生成器，强制ID唯一性校验                 |
| MCP消息体过大          | 客户端截断            | 严格限制单条消息大小，超过阈值强制分片返回     |
| 布局效果不理想         | 可视化体验差          | 针对不同图类型预设多套布局参数，支持前端微调   |

------

## 十二、下一步计划

本阶段1开发文档确认后，将进入**阶段2：任务拆解**，输出内容包括：

1. 工程初始化任务
2. 数据模型与Schema实现任务
3. 存储层实现任务
4. MCP服务实现任务
5. 布局引擎封装任务
6. HTTP API实现任务
7. 前端图表渲染实现任务
8. 索引服务实现任务
9. 联调与测试任务

每条任务将包含：任务编号、任务描述、涉及文件、变更类型、验收标准。