{
  "fragmentId": "dev-doc-f2",
  "docId": "dev-doc",
  "order": 2,
  "title": "四阶段MCP项目管理工具 - 开发文档(3/3)",
  "content": "(Worker)       │  │\n                                              │  │   elk计算节点坐标        │  │\n                                              │  └───────────┬─────────────┘  │\n                                              │              │                │\n                                              │  ┌───────────▼─────────────┐  │\n                                              │  │   本地文件存储层         │  │\n                                              │  │   项目目录/JSON文件      │  │\n                                              │  └─────────────────────────┘  │\n                                              └───────────────────────────────┘\n```\n\n### 5.2 核心数据流\n\n1. AI通过MCP发送操作请求 → MCP Server解析 → 调用对应业务服务\n2. 业务服务操作本地JSON文件 → 返回业务语义数据（无坐标）\n3. 前端请求图表渲染 → HTTP API调用布局引擎 → 输出带坐标的布局结果 → 前端SVG渲染\n4. 索引查询优先返回轻量摘要 → 按需再拉取完整片段\n\n------\n\n## 六、核心数据模型\n\n### 6.1 Project 项目实体\n\n```\ninterface Project {\n  projectId: string;\n  projectName: string;\n  currentStage: \"s1\" | \"s2\" | \"s3\" | \"s4\";\n  createdAt: number;\n  updatedAt: number;\n  stage1: {\n    docId: string;\n    diagramIds: string[];\n  };\n  stage2: {\n    taskDocId: string;\n    requirementIds: string[];   // 需求列表（需求下挂任务）\n    diagramIds: string[];\n  };\n  stage3: {\n    changeRecordIds: string[];\n  };\n  stage4: {\n    bugRecordIds: string[];\n  };\n  indexId: string;\n}\n```\n\n### 6.2 Requirement 需求实体（阶段2任务分组粒度）\n\n任务按「需求」这一更粗粒度分组管理，一个需求可关联一个 Git 分支，task 跟随分支切换。\n\n```\ninterface Requirement {\n  requirementId: string;\n  title: string;                       // 需求标题\n  description?: string;                // 需求描述\n  branchName?: string;                 // 关联 Git 分支名（可选）\n  status: \"active\" | \"done\" | \"archived\"; // 需求生命周期状态\n  taskIds: string[];                   // 该需求下的任务ID列表\n  createdAt: number;\n  updatedAt: number;\n}\n```\n\n- 层级：`Project → Requirement → Task`，需求为独立结构化实体\n- 需求关联 Git 分支：`branchName` 与仓库分支对齐，切分支时该需求及其 task 跟随切换\n- 需求状态独立：需求完成可 `done`/`archived` 归档，不影响其他需求\n- AI 读取路径：需求索引 → 需求内 task 摘要 → 单 task，Token 分层消耗"
}